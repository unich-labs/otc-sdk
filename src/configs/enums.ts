export enum EOfferType {
    Buy = 1,
    Sell = 2,
}

export enum EOfferStatus {
    Open = 1,
    Filled = 2,
    Canceled = 3,
}

export enum EOrderStatus {
    Open = 1,
    SettleFilled = 2,
    SettleCanceled = 3,
    Canceled = 4,
}

export enum ETokenStatus {
    Active = 1,
    Inactive = 2,
    Settle = 3,
}
