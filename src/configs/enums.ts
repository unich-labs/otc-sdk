export enum EMarketStatus {
    Active = 1,
    Inactive = 2,
    Settle = 3,
}

export enum EOrderType {
    Buy = 1,
    Sell = 2,
}

export enum EOrderStatus {
    Open = 1,
    Filled = 2,
    Canceled = 3,
}

export enum ETradeStatus {
    Open = 1,
    SettleFilled = 2,
    SettleCanceled = 3,
    Canceled = 4,
}
